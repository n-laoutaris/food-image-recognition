# Core scientific and ML stack
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
matplotlib>=3.4
seaborn>=0.11
torch>=1.10
torchvision>=0.11
timm>=0.6.12
Pillow>=8.3
tqdm>=4.62
tabulate>=0.8

# Jupyter for notebook support
notebook>=6.4
ipykernel>=6.0

# For compatibility with Windows multi-threading (OpenMP)
# (No extra package needed, but see code for KMP_DUPLICATE_LIB_OK)

# Optional: for reproducibility and random sampling
# (random, re, statistics are standard library)
